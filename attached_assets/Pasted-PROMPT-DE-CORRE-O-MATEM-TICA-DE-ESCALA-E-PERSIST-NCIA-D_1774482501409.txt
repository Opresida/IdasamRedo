PROMPT DE CORREÇÃO: MATEMÁTICA DE ESCALA E PERSISTÊNCIA (DRAG & DROP PDF)

Atua como um Desenvolvedor Front-end Sénior.
Temos um bug crítico no componente GerarPdfsTab do ficheiro client/src/pages/dashboard/capacitacao.tsx.

SINTOMAS:

Ao clicar em "Salvar Config", as configurações de blocos (texto, posições X/Y arrastadas) não estão a ser retidas para as gerações subsequentes.

Ao gerar a prévia ou baixar o ZIP, os PDFs saem em branco (as variáveis não aparecem). O texto está a ser desenhado fora dos limites visíveis do PDF.

PASSO A PASSO PARA CORREÇÃO RÍGOROSA:

1. Implementar Persistência Real ("Salvar Config"):
O botão <Button>Salvar Config</Button> deve persistir o estado blocks associado ao selectedCourseId no localStorage.

Cria a função handleSaveConfig: localStorage.setItem(\cert_config_${selectedCourseId}`, JSON.stringify(blocks))` e exibe um Toast de sucesso.

Adiciona um useEffect que observa a mudança do selectedCourseId: se houver um curso selecionado, tenta buscar a configuração guardada com localStorage.getItem(). Se existir, faz setBlocks(JSON.parse(saved)). Caso contrário, carrega o estado blocks predefinido/default.

2. Corrigir a Matemática de Escala (Crucial para o pdf-lib):
O texto está invisível porque o plano cartesiano do DOM é (0,0) no Top-Left, mas no PDF-LIB é (0,0) no Bottom-Left, e o tamanho do <Page> do react-pdf na tela é diferente das dimensões reais (width/height) extraídas do pdfDoc.
Dentro do handleGenerate (no loop onde faz o drawText), APLIQUE ESTA LÓGICA EXATA PARA CADA BLOCO:

// Passo A: Obter dimensões REAIS da página do PDF
const { width: pdfWidth, height: pdfHeight } = page.getSize();

// Passo B: Obter as dimensões do container do DOM onde o react-pdf está a ser exibido.
// Precisas de uma referência à div que encapsula a <Page> do react-pdf.
// Assumimos que tens algo como: const pdfWrapperRef = useRef<HTMLDivElement>(null);
const domWidth = pdfWrapperRef.current?.clientWidth || 800; // fallback seguro
const domHeight = pdfWrapperRef.current?.clientHeight || 600; // fallback seguro

// Passo C: Calcular o fator de escala de conversão (Tela -> PDF Real)
const scaleX = pdfWidth / domWidth;
const scaleY = pdfHeight / domHeight;

// Passo D: Para CADA bloco de texto a desenhar, aplica a conversão:
const finalSize = block.size * scaleY; // Ajusta o tamanho da fonte
const finalX = block.x * scaleX;
// INVERSÃO DO EIXO Y (pdfHeight - domY - tamanho da fonte):
const finalY = pdfHeight - (block.y * scaleY) - finalSize; 

// Desenha no PDF com as coordenadas convertidas
page.drawText(textoProcessado, {
  x: finalX,
  y: finalY,
  size: finalSize,
  font: fontSelecionada,
  color: rgb(0,0,0),
  maxWidth: 600 // Previne vazamento horizontal
});


3. Substituição Correta das Variáveis (Parser):
Certifica-te de que antes do page.drawText(), o texto passe por uma substituição garantida usando um Regex simples:

let textoProcessado = block.text;
textoProcessado = textoProcessado.replace(/{aluno}/g, student.fullName);
textoProcessado = textoProcessado.replace(/{curso}/g, course.title);
textoProcessado = textoProcessado.replace(/{carga}/g, course.workload.toString());
textoProcessado = textoProcessado.replace(/{data}/g, dataEmissao);
// etc...


INSTRUÇÕES FINAIS AO AGENTE:

Modifica o GerarPdfsTab para incluir o useRef na div que contém o <Document>/<Page> do react-pdf (ex: ref={pdfWrapperRef}). Isto é indispensável para calcular domWidth e domHeight.

Atualiza o evento de arrastar (Drag onStop) para gravar x e y puramente relativos a esse pdfWrapperRef principal (evita pegar o X/Y da viewport).

Não modifiques nada fora do componente GerarPdfsTab.